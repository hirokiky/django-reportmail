[tox]
envlist = py27, py33, flake8

[testenv]
deps = testfixtures
commands =
    pip install -e .
    django-admin.py test --settings=tests.settings


[testenv:flake8]
deps = flake8
commands = flake8 reportmail
